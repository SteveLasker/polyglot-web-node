version: '2'

services:
  api-dotnet:
    image: stevelasker/api-dotnet
  web-node:
    image: stevelasker/web-node:debug
    ports:
      - "3000:3000"
      - "5858:5858"
    volumes:
      - .:/src
    environment:
      - REMOTE_DEBUGGING
      - HTTPPORT=3000
      - APIPORT=5000
